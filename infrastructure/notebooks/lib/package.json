{
  "name": "openjii-python-packages",
  "version": "0.1.0",
  "description": "Python package build scripts for openJII",
  "scripts": {
    "build": "npm run build:multispeq && npm run build:ambyte && npm run build:enrich",
    "build:multispeq": "cd multispeq && python3 -m build",
    "build:ambyte": "cd ambyte && python3 -m build",
    "build:enrich": "cd enrich && python3 -m build",
    "clean": "npm run clean:multispeq && npm run clean:ambyte && npm run clean:enrich",
    "clean:multispeq": "cd multispeq && rm -rf dist/ build/ *.egg-info/",
    "clean:ambyte": "cd ambyte && rm -rf dist/ build/ *.egg-info/",
    "clean:enrich": "cd enrich && rm -rf dist/ build/ *.egg-info/"
  },
  "devDependencies": {},
  "private": true
}
