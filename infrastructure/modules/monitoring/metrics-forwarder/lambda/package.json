{
  "name": "metrics-forwarder",
  "private": true,
  "version": "1.0.0",
  "description": "Forwards Databricks heartbeat metric files from S3 into CloudWatch",
  "main": "index.js",
  "scripts": {
    "sync": "rm -rf ./lib && cp -R ../../../../../packages/monitoring/dist ./lib",
    "build": "npm run sync && npm ci --omit=dev && zip -Xr function.zip index.js package.json lib node_modules > /dev/null"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0"
  }
}
