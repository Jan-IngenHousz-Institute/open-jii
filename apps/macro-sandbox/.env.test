# Test port configuration — offset from dev ports (9001-9003)
# so tests can run while dev containers are up.
PYTHON_PORT=9011
JAVASCRIPT_PORT=9012
R_PORT=9013

# Isolate test containers from dev containers
COMPOSE_PROJECT_NAME=macro-sandbox-test
