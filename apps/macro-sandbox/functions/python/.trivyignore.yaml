# AWS Lambda Python base image CVEs (public.ecr.aws/lambda/python:3.12)
# AWS-managed components; suppressions are only needed while a CVE is fixed
# upstream but AWS has not yet republished the base image.
# Sandbox Lambdas are VPC-isolated with no internet access and no IAM credentials.
#
# Pruned 2026-08-05 against a freshly pulled base: AWS republished with the
# openssl/libsolv/libacl/glib2/sqlite/util-linux/gawk/rpm batches fixed and the
# runtime interface emulator rebuilt; no HIGH/CRITICAL findings remain. The base
# moves continuously, so a red scan here usually means a new CVE landed in a
# package awaiting the next AWS republish: verify reachability, add a justified
# entry with an expiry, and prune it once the republish lands.

vulnerabilities: []
