# R sandbox image (debian:bookworm-slim). Currently no HIGH/CRITICAL findings.
# The scan runs with ignore-unfixed, so unfixed-in-bookworm Debian CVEs never
# fail CI; an entry is only needed once Debian ships a fix we cannot take yet.

vulnerabilities: []
