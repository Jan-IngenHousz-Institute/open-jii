"""
Reference examples for the data_repair framework.

These files are NOT imported by the default registry (`data_repair.repairs`),
so the decorators do not auto-register. Copy-paste a file into
`data_repair/repairs/` (and add an import line in `repairs/__init__.py`) to
activate it as a real repair.

Each example file demonstrates a distinct scenario; pick the closest match
to your incident.
"""
