{
  "name": "data",
  "version": "0.1.0",
  "private": true,
  "description": "Databricks notebooks and Python packages for OpenJII",
  "scripts": {
    "check-python": "command -v python3 >/dev/null 2>&1 || (echo 'Python3 not found, skipping Python operations' && exit 1)",
    "check-poetry": "python3 -m poetry --version >/dev/null 2>&1 || (echo 'Poetry not found, installing...' && pip3 install poetry)",
    "install": "pnpm run check-python && pnpm run check-poetry && python3 -m poetry install",
    "build": "pnpm run check-python && pnpm run build:multispeq && pnpm run build:ambyte && pnpm run build:enrich && pnpm run build:openjii",
    "build:multispeq": "cd src/lib/multispeq && python3 -m build",
    "build:ambyte": "cd src/lib/ambyte && python3 -m build",
    "build:enrich": "cd src/lib/enrich && python3 -m build",
    "build:openjii": "cd src/lib/openjii && python3 -m build",
    "clean": "git clean -xdf .cache .turbo dist/ *.egg-info/ && pnpm run clean:multispeq && pnpm run clean:ambyte && pnpm run clean:enrich && pnpm run clean:openjii",
    "clean:multispeq": "cd src/lib/multispeq && git clean -xdf dist/ build/ *.egg-info/",
    "clean:ambyte": "cd src/lib/ambyte && git clean -xdf dist/ build/ *.egg-info/",
    "clean:enrich": "cd src/lib/enrich && git clean -xdf dist/ build/ *.egg-info/",
    "clean:openjii": "cd src/lib/openjii && git clean -xdf dist/ build/ *.egg-info/"
  }
}
