{
  "name": "multispeq-mqtt-interface",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "pyenv local 3.12 && pip install .",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "PYTHONPATH=src pyenv local 3.12 && python3 -m multispeq_mqtt_interface.main"
  }
}
