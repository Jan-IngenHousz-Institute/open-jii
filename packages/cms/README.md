# @repo/cms

Contentful integration for the public website — GraphQL clients with generated types, rich-text rendering, live preview support, and the CMS-driven page components (home hero, mission, key features, partners, footer, blog, FAQ, policies).

Regenerate GraphQL types after content-model changes:

```bash
pnpm --filter @repo/cms codegen
```
