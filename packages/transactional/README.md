# @repo/transactional

Transactional email templates for openJII, built with [React Email](https://react.email). Content is sourced from Contentful via `@repo/cms`; used by the backend and `@repo/auth` for verification, invitation, and notification emails.

Preview templates locally:

```bash
pnpm --filter @repo/transactional dev
```
