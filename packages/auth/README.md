# @repo/auth

Shared [Better Auth](https://better-auth.com) configuration for openJII — sessions, email OTP, API keys, and passkeys, with the Expo plugin for mobile. Uses `@repo/database` for storage and `@repo/transactional` for auth emails.

Consumed by the backend (server) and by the web and mobile apps (clients).
