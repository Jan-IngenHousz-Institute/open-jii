# Suppressions for the dependency-scan job (fs scan of pnpm-lock.yaml).
# Docker image scans read .trivyignore.docker-images instead; keeping the files
# separate scopes each suppression to the scan whose justification holds.
# Currently empty.
