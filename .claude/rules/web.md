---
paths:
  - "apps/web/**"
---

Before changing a screen, component or hook in the platform UI here, read `docs/standards/web.md`.
It has the rules, how each one is enforced, and the known debt. `docs/standards/code.md` and
`docs/standards/prose.md` apply everywhere as well.
