---
paths:
  - "apps/data/**"
---

Before changing a pipeline or a shared Python library here, read `docs/standards/data.md`. It has
the rules, how each one is enforced, and the known debt. `docs/standards/code.md` and
`docs/standards/prose.md` apply everywhere as well.
