---
paths:
  - "packages/api/**"
---

Before changing an endpoint's shape here, read `docs/standards/api.md`. It has the rules, how each
one is enforced, and the known debt. `docs/standards/code.md` and `docs/standards/prose.md` apply
everywhere as well.
