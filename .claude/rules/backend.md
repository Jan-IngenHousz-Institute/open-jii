---
paths:
  - "apps/backend/**"
---

Before changing a domain, use case, port, repository or controller here, read
`docs/standards/backend.md`. It has the rules, how each one is enforced, and the known debt.
`docs/standards/code.md` and `docs/standards/prose.md` apply everywhere as well.
