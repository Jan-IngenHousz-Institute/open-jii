---
paths:
  - "apps/macro-sandbox/**"
  - "apps/calibration-sandbox/**"
---

Before changing a runtime that executes code somebody else wrote, read
`docs/standards/sandboxes.md`. It has the rules, how each one is enforced, and the known debt.
`docs/standards/code.md` and `docs/standards/prose.md` apply everywhere as well.
