---
paths:
  - "apps/docs/content/**"
---

Before changing documentation a user reads here, read `docs/standards/docs-site.md`. It has the
rules, how each one is enforced, and the known debt. `docs/standards/code.md` and
`docs/standards/prose.md` apply everywhere as well.
